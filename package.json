{
  "name": "openrouter-audio-skill",
  "version": "1.0.0",
  "private": true,
  "description": "OpenRouter Audio OpenClaw skill CLI",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:bin": "node scripts/build-bin.mjs",
    "clean": "rm -rf build",
    "pretest:cli": "npm run build",
    "test:cli": "node --test test/cli.test.mjs",
    "test": "npm run test:cli"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "wasm-media-encoders": "^0.7.0"
  }
}
